# hpass - password obfuscator
 
CLI tool to obfuscate passwords. It uses algorithm argon2.

Usage:

```
> hpass reddit.com
Password: <enter-master-password>
```

It copies the generated password to clipboard.
See `hpass --help` for further details.


